name: DISABLED - Sync to Website

on:
  # Disabled: only manual dispatch possible, does nothing
  workflow_dispatch:

jobs:
  noop:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled workflow"
